interface:
  display_name: "koubo-clip"
  short_description: "Author and contract talking-head video edits"
  default_prompt: "Use koubo-clip to review and author a talking-head edit, then either render locally or export an immutable render contract for strict execution elsewhere."
